{% extends 'base.html.twig' %}

{% block title %}CritereSolution{% endblock %}

{% block body %}
    <h1>CritereSolution</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ critere_solution.id }}</td>
            </tr>
            <tr>
                <th>Point</th>
                <td>{{ critere_solution.point }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_critere_solution_index') }}">back to list</a>

    <a href="{{ path('app_critere_solution_edit', {'id': critere_solution.id}) }}">edit</a>

    {{ include('critere_solution/_delete_form.html.twig') }}
{% endblock %}
