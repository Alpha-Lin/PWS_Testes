<form method="post" action="{{ path('app_critere_solution_delete', {'id': critere_solution.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ critere_solution.id) }}">
    <button class="btn">Delete</button>
</form>
