{% extends 'base.html.twig' %}

{% block title %}Teste{% endblock %}

{% block body %}
    <h1>Teste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdTeste</th>
                <td>{{ teste.idTeste }}</td>
            </tr>
            <tr>
                <th>ImageTeste</th>
                <td>{{ teste.imageTeste }}</td>
            </tr>
            <tr>
                <th>TypeTest</th>
                <td>{{ teste.typeTest }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_teste_index') }}">back to list</a>

    <a href="{{ path('app_teste_edit', {'idTeste': teste.idTeste}) }}">edit</a>

    {{ include('teste/_delete_form.html.twig') }}
{% endblock %}
